name: core
description: ""
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
