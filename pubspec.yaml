name: pussy_chat
description: "Chats research Flutter project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  core_ui:
    path: ./core_ui
  domain:
    path: ./domain
  ui_kit:
    path: ./ui_kit
  core:
    path: ./core

flutter:
  uses-material-design: true
